services:
  - type: web
    name: smart-marketing-image-assistant
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install --omit=dev && python3 -m pip install --target ./python-vendor -r requirements.txt
    startCommand: npm start
    healthCheckPath: /functions/health
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: DASHSCOPE_API_KEY
        sync: false
      - key: MODELSCOPE_API_TOKEN
        sync: false
      - key: SILICONFLOW_API_KEY
        sync: false
      - key: CLOUDFLARE_ACCOUNT_ID
        sync: false
      - key: CLOUDFLARE_API_TOKEN
        sync: false
